name: obra_livre

description: "Obra Livre é uma plataforma para publicação, pesquisa e consulta de trabalhos académicos."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  go_router: ^14.0.0
  url_launcher: ^6.3.2
  file_picker: ^11.0.3
  supabase_flutter: ^2.0.0
  internet_connection_checker_plus: ^2.7.2
  hive_ce: ^2.11.3
  hive_ce_flutter: ^2.3.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
